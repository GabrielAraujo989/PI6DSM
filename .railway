# Railway Configuration File
# Este arquivo força o Railway a usar Docker builder com Root Directory

[build]
# Força o uso do Docker builder
builder = "dockerfile"

[service]
# Define o Root Directory para o projeto DetectFace
# Isso ativa o Docker builder em vez do Railpack
sourceDir = "./DetectFace"

[deploy]
# Configurações de deployment otimizadas
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Instruções para o Railway:
# 1. Use Root Directory = "./DetectFace"
# 2. Isso ativa automaticamente o Docker builder
# 3. O Dockerfile está localizado em ./DetectFace/Dockerfile
# 4. Esta configuração resolve o problema de detecção do Railpack